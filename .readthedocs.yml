version: 2

python:
    version: 3.8
    install:
        - method: pip
          path: .
          extra_requirements:
            - docs

# Let the build fail if there are any warnings
sphinx:
    builder: html
    fail_on_warning: true
